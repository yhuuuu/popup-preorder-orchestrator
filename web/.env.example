# Copy to .env and adjust if your API runs elsewhere.
#
# Note: VITE_-prefixed variables are compiled into the browser bundle and are
# readable by anyone who loads the page. Fine for a local dev token; a real
# deployment must keep the API token server-side.
VITE_API_BASE_URL=http://localhost:3000/api
VITE_API_TOKEN=dev-token
