# Environment Variables

# Bearer token for API authentication (required for all routes except /api/health)
API_TOKEN=dev-token

# Webhook secret for incoming callback security
WEBHOOK_SECRET=dev-webhook-secret
